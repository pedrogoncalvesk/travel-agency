import styled from "styled-components/native";

import { colors } from "../config/theme";

const ContainerPurple = styled.View`
  padding: 10px;
  background-color: ${colors.COLOR_PURPLE};
`;

export default ContainerPurple;
